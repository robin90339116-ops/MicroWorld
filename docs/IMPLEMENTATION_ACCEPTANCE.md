# 分阶段实现与验收记录

## 范围和提交规则

工作目录为主项目 `/Users/robinjack/Documents/MicroWorld`。旧 Claude 工作树不是当前实现基线。
第一阶段已按用户后续授权提交并推送 `833544a` 到 `origin/main`。
第二阶段改动保持独立，未包含在该提交中。
不能将接口返回成功、种子数据或模拟支付算作真实业务完成。

## 第一阶段：日常使用（代码修复中，尚待真机验收）

- 兴趣接口读取 `topics`；分类来自后端，搜索与分类参数接入，过期请求不覆盖最新筛选结果。
- 探索列表接入搜索，加载、失败重试和空列表不再显示默认地点卡片。
- 私聊加载真实消息列表并定时刷新，离开页面停止轮询；错误时保留草稿。
- 发送按钮防连点；同一草稿重试使用相同消息标识，后端拒绝冲突、避免重复写入。
- 登录令牌及安装标识使用鸿蒙 Asset 安全存储；不保存密码，不在健康检查或其他候选服务器传递令牌。
- 恢复会话会重新向原服务器验证；401 清理当前会话；退出要求服务端撤销成功并清除安全存储。
- 小世界名称、地点、主题、可见性绑定输入；创建失败留在表单；商家评价文本绑定提交。
- 聊天列表、历史、发送、好友详情、动态、连接请求增加账号范围校验；双方拉黑均禁止交互。

### 数据兼容性

旧 `friends` / `chats` 种子记录没有所属账号，不能安全推断归属，因此不展示给真实账号，未删除原记录。
真实好友记录需 `ownerId`，双人聊天需 `participantIds`。第二阶段的双方 NFC 确认流程已创建该结构。
不自动把全局演示人物迁移为用户好友。

### 验证命令

- 后端：`npm test`；隔离文件模式：`SMALLWORLD_DATABASE_URL= SMALLWORLD_CHAIN_PROVIDER=certificate npm run smoke`。
- 鸿蒙：使用 DevEco 安装目录内的 Node、Hvigor，指定 `DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk`，执行 `--mode module -p product=default -p module=entry@default assembleHap --no-daemon`。
- 编译和签名成功不等于运行验收。NFC 设备能力和第三方地图依赖仍有编译警告。

### 真机验收清单（均未执行，不得标为已通过）

第一阶段本地结果：`npm test` 10 项通过；隔离 JSON 模式冒烟测试通过；鸿蒙 `assembleHap`（含 SignHap）通过。
设备发现返回 `[Empty]`，当前未连接可用真机，因此未执行安装、重启恢复、界面走查和双机测试。
这些结果不覆盖云数据库、真实支付、地图真机出图和 BCS。

1. 注册、完善身份、重启 App 恢复同一账号；令牌过期返回登录。
2. 退出后重启不自动登录；服务端不可用时退出明确失败，不显示成功。
3. 兴趣分类、搜索、无结果、断网重试；快速切换后只显示最新条件结果。
4. 两个已建立真实关系的账号互发消息，重进查看历史；断网草稿保留，重试不重复。
5. 第三个账号不可读取前两者对话；拉黑后双方均不能继续交互。
6. 自定义世界名称、地点、主题、可见性真实保存；失败时输入不丢失。
7. 商家评价的文字必须出现在后端记录中（商家真实性和支付仍待第三阶段）。

## 第二阶段：个人碰一碰（代码与本地验证，不等于双机验收）

- 状态：waiting → awaiting_host → confirmed；另有 cancelled / expired / superseded。
- 读取方提交令牌和挑战并确认；被碰方必须使用本机 HCE 观察到的同一挑战显式确认。
- 首次及后续碰面均要求双方确认，暂不启用熟人免确认；同一对用户每 UTC 日只发放一次积分/好感度奖励。
- 两端轮询各自可见的会话结果，共享 proofId / chatId；取消、过期不产生奖励。
- 按账号建立两条好友记录和一个双人会话；奖励各自保存，可重复查询且不重复发放。
- 历史接口剔除设备标识、令牌和挑战；界面读取真实历史。
- 当前验证级别为 mutual-client-reported-nfc，hardwareVerified / gpsVerified 均为 false；不允许据此绕过硬件认证地点评价门槛。
- 徽章仍为未上链的数字凭证，不声称真实 NFT 铸造完成。
- 确认路由在读取请求正文之后再取得数据快照，避免单进程并发重试覆盖结果；多实例数据库事务仍待第四阶段。
- 真机清单：首次双方确认、取消与超时、重复读取、已是好友复次碰面、双方历史和奖励一致、跳转聊天、断网后恢复结果。

### 重启恢复

- 安全存储记录账号、后端地址、会话、角色、本机 NFC 挑战和用户操作意图；不会跨账号或后端恢复。
- 发起、读取、确认前写入检查点；写入失败不发送确认。存储写入串行化，旧会话清理不会删除新会话记录。
- 启动登录恢复或重新登录后查询原服务器；个人碰一碰记录页也提供手动恢复入口。
- 不因本机检查点过期就丢弃结果：服务器可能在截止前已完成确认，恢复时只读取奖励快照，不再次发放。
- 被碰方等待确认时仍需显式操作，检查点不能替代双方确认。读取方尚未绑定的会话返回 404 时保留本机读取记录，要求重新核对本人后提交。
- 取消意图跨重启保留；网络失败不显示服务端已取消。已完成但本地记录清理失败时保留成功结果，后续恢复仍是只读核对。
- 退出页面为取消；组件销毁只停止本机 NFC 和轮询，保留恢复记录。
- 限制：会话创建响应到达并写入检查点前即被杀进程时无法恢复该未知会话 ID；该孤立会话由服务端两分钟有效期失效，不会自动创建好友或奖励。

### 第二阶段验证

- `npm test`：18 项通过（含双方确认、幂等、账号隔离、取消/过期和服务端数据快照恢复）。
- 最终 `assembleHap`（含 SignHap）通过，NFC 设备能力和第三方地图依赖的既有编译警告仍存在。
- `npm run smoke`：隔离 JSON 模式通过，覆盖 HTTP 并发重复确认以及确认后双方发送/读取同一聊天消息。
- `entry/tests/session-vault.test.cjs`：4 项通过。使用已安装 SDK TypeScript 编译真实 SessionVault 代码并注入模拟 Asset API，验证检查点恢复、账号/服务端范围、写入顺序和失败后重试；不是系统安全存储的真机测试。
- 运行存储测试：设置 `HARMONY_TYPESCRIPT_PATH` 为 SDK 的 `ets/build-tools/ets-loader/node_modules/typescript` 绝对路径，执行 `node --test entry/tests/session-vault.test.cjs`。
- 真机 NFC、系统存储和杀进程恢复未实测；不得把以上自动测试表述为双机验收通过。

## 第三阶段 A：关闭模拟支付与商品评价资格漏洞

- 支付接口不再根据客户端金额直接创建 paid 订单；在支付渠道未接入时返回 503 / PAYMENT_NOT_CONFIGURED，不写入订单或改变碰触状态。
- 商品评价返回 403 / VERIFIED_PAYMENT_REQUIRED；历史模拟 paid 订单、客户端成功标志和重复请求均不能解锁评价。旧数据保留，不删除、不冒充真实交易。
- 前端移除固定金额模拟支付调用；入口明确显示未开通及不会扣款，商品评价按钮禁用。服务端同样拦截，不能通过绕过界面解锁。
- 新增支付安全回归测试，覆盖伪造支付成功、异常金额、重复请求、历史模拟订单、账号隔离和零写入；HTTP smoke 改为验证拒绝及无新增订单/商品评价。
- 此子阶段仅修复支付误判，不代表真实收款已接入。商家身份、真实 NFC 凭证与地点评价奖励仍未完成安全改造，当前商家到店演示不得用于生产发放权益。
- 后续接入必须包括服务端商品/金额确定、支付平台验签与查单、订单绑定、幂等回调及退款资格处理；不得只添加客户端支付成功标志重新开放。
- 本次验证：22 项后端单元测试通过；隔离 JSON 模式 HTTP smoke 通过；鸿蒙 assembleHap（含 SignHap）通过。既有 NFC 设备兼容性及地图依赖警告仍在，未进行真机支付验收。

## 第三阶段 B：演示评价与奖励幂等（非真实到店凭证验收）

- 地点评分必须为数值型 1–5 整数；标签最多 8 个、每个 1–24 字，评论最多 240 字。非法输入拒绝，不自动改成五星或静默截断。
- 相同评价重试返回 200 及原评价/奖励快照；不同内容重复提交返回 409。标签排序与重复标签不构成新评价。旧评价无奖励快照时返回 null，不重新铸造或补发。
- 奖励快照保存于评价与碰触记录，可在查询中恢复。余额为该次结算快照，不冒充当前余额。
- 暂定同账号、同地点每 UTC 自然日只奖励一次；新碰触可记录评价但奖励为零，界面不再兜底显示 +5 或虚构徽章。此为防刷默认规则，业务时区/活动策略后续可配置化。
- 重复打卡不刷新原打卡时间，也不会把已评价/已领奖状态回退。paid/rewarded 等状态不能替代有效打卡时间。
- 商家变更路由在完整读取正文后取得最新数据快照；用户设备字段取登录会话，忽略正文伪造的用户设备。仅覆盖单进程，尚无多实例数据库事务保证。
- 删除前端“已认证商家”“GPS 匹配”等虚假成功提示，入口明确为演示；新记录及旧记录的公开表示均不声称硬件/GPS 核验。
- **仍未完成：商家可信登记、真实 NFC 凭证交换、到店真实性和生产权益资格。现有演示 API 仍能创建演示打卡与演示积分，不应对外作为真实权益服务上线。**
- 新增重启快照恢复、重复与冲突提交、跨账号隔离、输入校验、打卡状态不回退、跨碰触每日限额及路由读取顺序测试；HTTP smoke 覆盖 6 路并发重试。
- 验证结果：31 项后端单元测试通过；隔离 JSON 模式 HTTP smoke 通过；鸿蒙 assembleHap（含 SignHap）通过，既有设备兼容性和地图依赖警告仍在。无真机 NFC / 商家身份验收结论。

## 第三阶段 C：登记终端与到店凭证服务端

- 商家授权来自服务器管理员维护的账号/设备/地点登记文件，不开放自助升权接口；缺失或异常配置拒绝授权。
- 新增终端列表、两分钟一次性会话、商家观察挑战上报；消费者凭证须匹配令牌摘要、商家上报挑战及登录设备。一次消费、重复返回同一 tap，不接收任意地点或商家覆盖登记信息。
- 旧演示创建请求被拒绝；旧演示打卡与评价资格关闭。登记撤销、会话超时或被替代均阻止新消费；首次打卡有 20 分钟证据窗口。
- 本阶段是服务端交付，App 商家 NFC 双端尚未接入，因此商家演示按钮改为未开放说明，不再调用旧的伪碰触请求。个人 NFC 路径未改。
- 验证等级明确为登记终端的客户端报告，不声称硬件认证、GPS 校验或防远程串通。配置及后续接入协议见 backend/docs/MERCHANT_TERMINAL_SETUP.md。
- 本节替代前一阶段中“演示 API 仍可创建打卡/奖励”的状态说明；旧数据保留，不伪装为新有效凭证。
- 验证：40 项后端测试通过；隔离 JSON 与临时登记文件的 HTTP smoke 通过，包含六路并发凭证兑换及评价重试；鸿蒙 assembleHap/SignHap 通过。既有兼容性警告仍在，未完成商家双机真机验收。

## 第三阶段 D：App 商家 NFC 双端接入

- 商家登录后查询自身账号及设备的登记列表，选择具体终端发起 HCE；消费者通过 ISO-DEP 读取，显式确认后才提交商家凭证。原伪碰触请求删除。
- 商家自动上报本机挑战，失败保留本页记录并提供重试；消费者用原凭证重试，仍由服务端校验登记、挑战、会话和设备。不把网络成功当作支付/硬件认证。
- NFC payload 增加 purpose：旧个人主机默认 personal，商家为 merchant。个人/商家界面拒绝相反类型，不交叉建立关系。
- 停止、切换类型、离开 NFC 页及组件销毁会清理本机 NFC 与计时器；服务端未消费会话依赖两分钟超时，不显示已经远程撤销。
- 原生读写回调与界面请求均添加代次校验，过时读取不进入新流程；打卡/评价响应不在退出或切换账号后跳回旧页面。
- 此节替代上一阶段“商家 App 尚未接入、入口未开放”的状态；代码入口现已开放，但须后台登记并进行真机验收。
- 尚未完成：商家跨进程安全检查点/恢复入口、服务端取消和商家消费结果同步展示、真实运营登记、两机实测；不能宣称完整生产闭环已验收。
- 本次验证：40 项后端测试、隔离 HTTP smoke、10 项前端协议/存储测试全部通过；最终 assembleHap（含 SignHap）通过。前端测试编译真实 ETS 后注入模拟 NFC/Asset，不是真机证明；hdc 设备列表为空。既有 NFC 能力及地图依赖警告仍存在。

## 第三阶段 E：商家恢复、服务端取消与参与者进度

- 服务端提供账号/设备范围内的会话查询和显式取消。未绑定读取方须凭 NFC 令牌及挑战取消；取消先于兑换则不能创建 tap，兑换先完成则取消返回 409 并保留原结果。
- 商家进度只公开已兑换/已打卡/已评价，不泄露消费者评价和余额；消费者读取原奖励快照。超过原会话截止时间仍能恢复已提交结果。
- App 使用独立 Asset 商家检查点，保存角色、会话、账号、原后端、令牌、挑战和取消意图。上报/确认/取消前写入失败即不发送相应操作；队列写入及按账号/后端/会话条件清除防止迟到清理误删。
- 登录后提示有恢复记录；用户从商家页或个人碰一碰记录页手动恢复，不自动替消费者确认。商家可恢复 HCE 等待或重试本机挑战上报，已兑换读取方继续打卡/评价或查看原奖励。
- 在商家会话页轮询双方进度，暂停/退出停止轮询。暂停本机、请求服务端取消、归档已结束本地记录分别提供按钮；不混用三者的成功含义。
- 本节替代上一阶段“尚未实现商家安全恢复、服务端取消和结果同步”的状态。代码及自动回归不等于双机/杀进程实测。
- 已知边界：会话响应尚未保存检查点时被杀进程的窗口只能超时失效；一个安装只保存一个账号/后端绑定的商家检查点；归档后历史仍在服务端，但不从本检查点恢复。多实例事务及生产权益仍未验收。
- 验证结果：46 项后端测试、21 项前端逻辑/协议/存储测试通过，HTTP smoke 覆盖四路并发取消、原奖励快照与双方进度隔离；最终 assembleHap/SignHap 通过。界面恢复测试执行从真实 Index.ets 提取的方法并注入模拟 API，协议/Asset 也为模拟，不等于双机与杀进程真机验收。既有 NFC/地图兼容性警告保留。

## 第四阶段 A：本地状态文件可靠性

- 文件保存改为同目录临时文件、文件同步、原子替换与目录同步。写入中断前旧文件不被截断；替换后同步不确定则明确报错并要求核对结果。
- 空文件、损坏 JSON、错误根结构、读失败或运行中文件消失均不自动覆盖重建；已有坏数据保留。运行中返回存储错误 503，启动时拒绝带坏文件启动。
- 初始路径不存在仍会建立种子文件；原子保存不是备份或多进程事务，无法区分完整重启前的数据丢失与首次部署。运维边界见 backend/docs/STORAGE_RELIABILITY.md。
- 55 项后端测试通过；新增真实子进程 storage smoke 验证注册/会话跨重启、损坏/丢失不重置及显式恢复测试备份后登录；已有完整接口 smoke 通过。未改前端，因此本次未重新构建 HAP。
- Postgres 异步持久化及多实例问题尚未修复，华为云尚未真实连接；本次不能表述为云数据库部署完成。

## 第四阶段 B：数据库事务与成功响应确认

- 本次回归：68 项后端测试、文件模式完整 HTTP smoke、真实子进程 storage smoke 全部通过。没有改动前端，本次未重新构建 HAP；以下数据库测试仍使用模拟连接，不代表真实数据库验收。

- 移除后台异步落库和立即重复重试。请求读取最新状态并取得行锁，业务处理完成后 UPDATE/COMMIT，提交确认前不发送 HTTP 成功响应或注册令牌。
- 请求异常回滚，连接/查询失败返回 503；提交回执丢失明确为不确定结果，不自动重新执行奖励/订单操作。默认 TLS 校验证书，限制连接/查询/锁等待时间，并先排空请求再关连接池。
- 新增隔离数据库 smoke 入口，无连接地址时明确失败，不退回文件模式；临时表随机命名并清理，不使用调用者提供的业务表名。
- 模拟数据库及真实 HTTP 处理代码测试覆盖等待 COMMIT、更新失败、丢失提交回执、两个存储实例并发、回滚、慢上传不持锁、TLS 和非法配置。模拟不等于实际 PostgreSQL 测试。
- 本机没有 pg、PostgreSQL/Docker；真实数据库、云端 TLS、多进程并发和 openGauss 兼容性仍待验收。JSON 单行全局锁也不是面向大规模并发的最终数据模型。

## 第四阶段 C：真实数据库验收准备（未完成实库验收）

- 新增 pg 实库多进程、重启读取、回滚、锁超时和状态异常测试入口；每次独立随机表，子进程限制时长，只清理本次测试表。
- 安装 pg、提交依赖锁文件并将 Docker 安装切换为 npm ci；本地数据库示例端口只绑定回环地址，防止示例账号暴露局域网。
- GitHub PostgreSQL 16 自动验收模板已准备但未启用：当前凭据缺 workflow 权限；本机 PostgreSQL 安装又被旧 Xcode 工具链阻挡。未升级 Xcode、未擅自切换 GitHub 账号，未连接华为云或业务数据。
- 本次验证：71 项后端测试、文件模式完整 HTTP smoke、子进程 storage smoke 通过；新增 3 项入口保护测试不属于实库测试。模板 YAML 语法检查通过但未执行；未改前端，未重新构建 HAP。

## 后续阶段（未完成）

本次新增交付见第四阶段 C，以下外部验收仍未完成。

- 第二阶段：两部真机实际 NFC 交换、杀进程恢复与全流程验收。
- 第三阶段：真实商家身份及 NFC 凭证；选定支付渠道和商户资质后接入支付创建、回调验签、查单、退款与评价资格处理；奖励幂等。
- 第四阶段：云数据库持久性与并发改造、真实云连接、MapKit 真机出图和真实位置、BCS 真正铸造与回执查询。
- AR / AI 独立安排，不属于本阶段完成项。

## 外部依赖

尚需确认真实支付渠道/商户资质、云数据库与 BCS 资源、两部可调试鸿蒙真机。
密码、私钥、证书只通过本机安全配置提供，不写入文档、聊天或 Git。
最终推送前需检查已有项目签名配置是否含本机凭据，不将其作为交付内容公开。
